package www.callerid.com.androidcalleridcloudrelay.Classes;

// Simple interface to allow for possible callbacks between service and activity.
public interface ServiceCallbacks{
    void getUDP(String rString);
}
